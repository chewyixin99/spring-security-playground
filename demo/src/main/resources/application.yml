server:
    port: 8080

spring:
    datasource:
        url: jdbc:mysql://localhost:3306/user_registration # port of MYSQL
        username: root
        password: root
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        show-sql: true # queries will be printed
        hibernate:
            ddl-auto: update # change will be reflected